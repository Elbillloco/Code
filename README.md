
/*Al correr el codigo, imprime en consola 35014 por que
Detecta las comillas simples de multa como un String 
Lo traduce como un Int*/

/*Responder: Hubo algun error, alguna advertencia o algo en la consola que avisara?
Respuesta: Si, como tal el error esta en la impresion en consola, no hace
La operacion matematica correctamente, sino que lo concatena como un string*/

/*Responder: Si el archivo tiene un error de tipos, por que node lo ejecuta?
Cual comando revisa y cual ejecuta?
Respuesta : Node ejecutara el codigo independientemente si hay un error, correra
Hasta donde el codigo esta correctamente*/

/*valor_numerico_1 = 1; Esto no puede suceder debido a que const es solo para valores
que no cambiaran o que seran fijos*/

/*Responder: de las dos lineas que usan const, Por que solo una falla?
Respuesta: esta falla por que const no puede modificarse una vez declarado, esto debido a
que es una constante, la naturaleza y lo que no permite TS es la modificacion de este.

Responder: al asignarle un texto a la variable let, nadie escribio que fuera un numero, De donde salio ese tipo?
Respuesta: al ser una variable Let, este tipo cambia dependiendo del valor que se le asigne, ejemplo, si el valor tiene
comillas, se hara un string, si es numerico simple, se hara Integer, mientras que si se un true o false, sera booleano.*/

